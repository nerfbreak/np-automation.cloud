# 🔒 Locked Logic Registry

Production-verified code sections that **MUST NOT be modified** without explicit owner approval.

## Rules

1. Code between `🔒 LOCKED LOGIC` and `🔒 END LOCKED LOGIC` markers is frozen
2. No refactoring, "cleanup", optimization, or "improvement" of locked sections
3. If new behavior needed → create a **new separate function**, do not modify locked one
4. Locked sections can only be unlocked by explicit owner instruction in chat

---

## Locked Sections

### 1. Inventory Master Extraction

| Field | Value |
|-------|-------|
| **File** | `src/lib/newspage-bot.ts` |
| **Functions** | `extractNewspageStock()`, `parseInventoryCsv()` |
| **Lines** | Between `🔒 LOCKED LOGIC — INVENTORY MASTER EXTRACTION` markers |
| **Locked date** | 2026-07-23 |
| **Verified by** | Production run — GOOD_WHS filter confirmed, BAD_WHS excluded |

**What's locked:**
- Login + session handling flow
- Export job creation (Add → Next → General config → Next → Disclaimer)
- Interface ID popup selection (`INTF_ID_SelectButton` → `pop_Dynamic` → search → click)
- Dynamic field configuration: File Type=D, Separator=Tab, Warehouse=GOOD_WHS, Status=Active
- Exact element IDs: `grd_DynamicFilter_ctl02_dyn_Field_txt_Value`, `grd_DynamicFilter_ctl07_dyn_Field_drp_Value`
- Add → Save → Run → poll job → download CSV
- CSV parsing logic (header detection, column mapping)

**Why locked:**
- Popup flow is the **only** way to trigger ASP.NET postback that renders `grd_DynamicFilter`
- Direct fill `INTF_ID_Value` + Tab does NOT work (no postback, no dynamic fields)
- Previous 6+ failed attempts trying alternative approaches confirmed this
